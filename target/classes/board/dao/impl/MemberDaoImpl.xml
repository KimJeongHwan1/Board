<?xml version="1.0" encoding="UTF-8"?>

<!-- 마이바티스 3 Mapper DOCTYPE -->
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board.dao.face.MemberDao">
<select id="getMember_codeDao" parameterType="String" resultType="int">
	SELECT code FROM member
		WHERE id = #{loginid }
</select>

<select id="getMember_name" parameterType="String" resultType="String">
	SELECT name FROM member
		WHERE id = #{loginid }
</select>

<insert id="member_insert" parameterType="board.dto.Member">
	INSERT INTO member ( code , id , pw , name , admin )
	VALUES ( nextval('mem_seq') , #{id} , #{pw} , #{name} , 0 )
</insert>

<select id="selectByMember" parameterType="board.dto.Member" resultType="board.dto.Member">
		SELECT * FROM member
		WHERE id = #{id }
</select>

<select id="selectCntLogin" parameterType="board.dto.Member" resultType="int">
		SELECT COUNT(*) FROM member WHERE id = #{id } AND pw = #{pw }
</select> 

<select id="joinCheckid" parameterType="String" resultType="int">
		select count(*) from member where id = #{id }
</select> 
</mapper>